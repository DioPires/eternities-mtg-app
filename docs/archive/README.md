# Archived documents

Superseded by their successors, kept because the reviews that accepted the current documents cite
them. Nothing here is current:

- `prd.md`, `prd_v2.md` — superseded by `prd_v3.md`, the contract the implementation plan builds to.
- `prd-review.md` — the adversarial review that cleared `prd_v3.md`.
- `implementation-plan-review.md` — the review that failed plan revision v1; v2 fixes every
  accepted finding.

`review/phase-3/*.png` (15 MB of Phase 3 captures) was deleted in the same change. The captures are
in git history and reachable from the Phase 3 review; `scripts/visual-gate.mjs` regenerates the
equivalent on demand.
